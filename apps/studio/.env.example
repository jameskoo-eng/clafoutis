# GitHub personal access token for development (bypasses OAuth, avoids rate limits)
# Create one at https://github.com/settings/tokens with "repo" scope
VITE_GITHUB_TOKEN=ghp_your_personal_access_token

# GitHub OAuth App credentials (for the Sign In with GitHub flow)
# Create an OAuth App at https://github.com/settings/developers
# Set the callback URL to http://localhost:5174/callback
VITE_GITHUB_CLIENT_ID=your_github_oauth_app_client_id
VITE_GITHUB_REDIRECT_URI=http://localhost:5174/callback

# Backend API URL (leave empty for local dev — Vite proxy handles it)
# Set to your production backend URL when deploying to Vercel
# e.g. https://clafoutis-server.up.railway.app
VITE_API_URL=
