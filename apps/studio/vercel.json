{
  "buildCommand": "cd ../.. && pnpm install && pnpm turbo build --filter=@clafoutis/studio",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
