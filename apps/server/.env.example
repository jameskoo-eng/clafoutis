# ---- Required ----

# GitHub OAuth App credentials
# Create an OAuth App at https://github.com/settings/developers
# Set the callback URL to match VITE_GITHUB_REDIRECT_URI in apps/studio/.env
GITHUB_CLIENT_SECRET=your_github_oauth_app_client_secret

# ---- Optional ----

# Server port (default: 3001)
PORT=3001

# Frontend origin for CORS (required in production)
# e.g. https://studio.clafoutis.dev
FRONTEND_URL=

# Set to "false" when the frontend is deployed separately (e.g. Vercel)
# Default: true — serves apps/studio/dist as static files with SPA fallback
SERVE_STATIC=true

# Node environment (development | production)
NODE_ENV=development
