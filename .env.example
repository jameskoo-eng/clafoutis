# GitHub Personal Access Token for CLI tests and private repository access
# This token is used by the CLI to authenticate GitHub API requests and avoid rate limits
# Create a token at https://github.com/settings/tokens/new (classic) with 'repo' scope
# Or use: gh auth token (to get your current GitHub CLI token)
CLAFOUTIS_REPO_TOKEN=ghp_your_token_here
